name: flutter_app
description: "O aplicativo de monitoramento de exercícios FitAI."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  fl_chart: ^0.64.0
  camera: ^0.10.5+5
  flutter_markdown: ^0.6.18
  
  flutter_launcher_icons: ^0.13.1

  flutter_native_splash: ^2.4.0

  table_calendar: ^3.1.2

  image_picker: ^1.1.2

  intl: ^0.19.0 
  google_sign_in: ^6.2.1

  cupertino_icons: ^1.0.2
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png" 

flutter_native_splash:
 
  color: "#FFFFFF"  
 
  image: assets/images/icon.png

  
  android: true
  ios: true